# 设置端口
server:
  port: 9000

# 配置数据库源
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.1.125:3306/gulimall_oms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置nacos 注册中心
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.125:8848
  application:
    name: gulimall-order

# MapperScan
# sql映射文件位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  #主键自增
  global-config:
    db-config:
      id-type: auto
